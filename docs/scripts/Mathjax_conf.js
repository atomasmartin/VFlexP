MathJax = {
  tex: {
    inlineMath: [['\\(', '\\)']]
  },
  options: {
    enableMenu: true,
    menuOptions: {
      settings: {
        zoom: 'Click', // Set 'Click' for click-to-zoom
        zscale: '200%', // Optional: Set the zoom scale (e.g., 200%)
      }
    }
  }
};